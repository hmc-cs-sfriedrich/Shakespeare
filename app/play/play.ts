export class Play {
    title: string
    fileName: string
    playIndex: number

    constructor(title: string, fileName: string, playIndex: number) {
        this.title = title;
        this.fileName = fileName;
    }
}
export class ActScene {
    act: number
    scene: number;
    sceneIndex: number;

    constructor(act: number, scene: number, sceneIndex: number) {
        this.act = act;
        this.scene = scene;
        this.sceneIndex = sceneIndex;
    }
}